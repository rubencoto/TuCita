{
  "name": "tucita-heroku-root",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "heroku-postbuild": "cd TuCita/ClientApp && npm install && npm run build"
  }
}
