# ============================================
# TuCita - Variables de Entorno
# ============================================
# Copia este archivo a .env y completa los valores
# NUNCA commits el archivo .env a Git
# ============================================

# ============================================
# BASE DE DATOS (SQL Server / AWS RDS)
# ============================================
DB_SERVER=tcodb.cleoiq2ws06y.us-east-2.rds.amazonaws.com
DB_PORT=1433
DB_NAME=tco_db
DB_USER=tcoadmin
DB_PASSWORD=TCO2025**

# ============================================
# JWT (Autenticación)
# ============================================
# La clave DEBE tener mínimo 32 caracteres
JWT_KEY=your-super-secret-jwt-key-minimum-32-characters-long-for-security
JWT_ISSUER=TuCita
JWT_AUDIENCE=TuCitaUsers

# ============================================
# AWS S3 (Almacenamiento de Archivos)
# ============================================
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=tucita-medical-documents

# ============================================
# EMAIL (SMTP)
# ============================================
# Ejemplo con Gmail:
# - Habilita "Acceso de apps menos seguras" o usa "Contraseña de aplicación"
# Ejemplo con SendGrid, Mailgun, etc: usa sus credenciales SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_SENDER=no-reply@tucitaonline.org

# ============================================
# APLICACIÓN
# ============================================
ASPNETCORE_ENVIRONMENT=Development
# En producción Heroku, esta variable se configura automáticamente:
# PORT=8080 (o el que Heroku asigne)

# ============================================
# HEROKU (Solo para producción)
# ============================================
# Estas variables se configuran directamente en Heroku
# No las incluyas en el archivo .env local
# Usa: heroku config:set VARIABLE=valor -a tucita-app

# ============================================
# NOTAS DE SEGURIDAD
# ============================================
# 1. NUNCA compartas este archivo
# 2. NUNCA lo subas a Git (.env está en .gitignore)
# 3. Usa contraseñas fuertes y únicas
# 4. Rota las credenciales regularmente
# 5. En producción, usa variables de entorno de Heroku
