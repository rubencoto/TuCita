# ==============================================
# TuCita - Configuración de Variables de Entorno
# ==============================================
# IMPORTANTE: Copia este archivo como ".env" y completa con tus credenciales reales
# NUNCA subas el archivo .env a git (ya está en .gitignore)

# ==============================================
# AZURE SQL DATABASE (REQUERIDO)
# ==============================================
# Servidor Azure SQL (formato: nombreservidor.database.windows.net)
DB_SERVER=tu-servidor.database.windows.net

# Puerto de SQL Server (por defecto 1433)
DB_PORT=1433

# Nombre de la base de datos
DB_NAME=TuCita

# Usuario administrador de la base de datos
DB_USER=tu-usuario-admin

# Contraseña del usuario (debe cumplir requisitos de Azure)
# Mínimo 8 caracteres, mayúsculas, minúsculas, números y símbolos
DB_PASSWORD=Tu-Contraseña-Segura-123!

# ==============================================
# JWT AUTHENTICATION (REQUERIDO)
# ==============================================
# Clave secreta para firmar tokens JWT
# DEBE tener al menos 32 caracteres
# Genera una clave segura: https://randomkeygen.com/
JWT_KEY=tu-clave-jwt-super-secreta-minimo-32-caracteres-de-longitud

# Emisor del token (nombre de tu aplicación)
JWT_ISSUER=TuCita

# Audiencia del token (quién puede usar el token)
JWT_AUDIENCE=TuCitaUsers

# Tiempo de expiración del token en minutos (60 = 1 hora)
JWT_EXPIRY_MINUTES=60

# ==============================================
# EMAIL SMTP (OPCIONAL - Para desarrollo)
# ==============================================
# Servidor SMTP (Gmail, SendGrid, Mailgun, etc.)
SMTP_SERVER=smtp.gmail.com

# Puerto SMTP (587 para TLS, 465 para SSL)
SMTP_PORT=587

# Usuario del servidor SMTP
SMTP_USERNAME=tu-email@gmail.com

# Contraseña o App Password del servidor SMTP
# Para Gmail: https://support.google.com/accounts/answer/185833
SMTP_PASSWORD=tu-contraseña-app

# Email del remitente
SMTP_FROM=noreply@tucita.com

# ==============================================
# AZURE BLOB STORAGE (OPCIONAL - Futuro)
# ==============================================
# Cuenta de almacenamiento de Azure
# AZURE_STORAGE_ACCOUNT=tucitastorage

# Clave de acceso de la cuenta de almacenamiento
# AZURE_STORAGE_KEY=tu-clave-de-almacenamiento

# Container para documentos clínicos
# AZURE_STORAGE_CONTAINER=documentos-clinicos

# ==============================================
# CONFIGURACIÓN DE ENTORNO
# ==============================================
# Entorno de ejecución (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development

# Puerto de la aplicación (opcional)
# ASPNETCORE_URLS=http://localhost:5000;https://localhost:5001

# ==============================================
# NOTAS IMPORTANTES
# ==============================================
# 1. Este archivo NO debe subirse a control de versiones
# 2. Cada desarrollador debe tener su propio archivo .env
# 3. En producción, usa Azure Key Vault o variables de entorno del servidor
# 4. Las contraseñas deben cumplir los requisitos de seguridad de Azure
# 5. Regenera JWT_KEY para cada entorno (dev, staging, prod)

# ==============================================
# PASOS PARA CONFIGURAR
# ==============================================
# 1. Copia este archivo: cp .env.example .env
# 2. Completa las variables DB_* con tus credenciales de Azure SQL
# 3. Genera una clave JWT segura de al menos 32 caracteres
# 4. (Opcional) Configura SMTP para envío de emails
# 5. Ejecuta: dotnet run --project TuCita
# 6. Verifica en los logs que la conexión sea exitosa

# ==============================================
# RECURSOS ÚTILES
# ==============================================
# Azure SQL Database: https://portal.azure.com
# Generar claves seguras: https://randomkeygen.com/
# Gmail App Passwords: https://support.google.com/accounts/answer/185833
# Entity Framework Migrations: https://docs.microsoft.com/ef/core/managing-schemas/migrations/
